<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/textview_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="请输入您的生日......" />

    <!-- DatePicker常见属性
    1.calendarViewShown:是否显示日历栏
    2.android:endYear:用于设置最终的月分
    3.android:maxDate:用于设置最大的日期
    4.android:minDate:用于设置最小的日期
    5.android:spinnersShown:用于设置spinner是否显示。
    6.android:startYear:用于设置开始年份
     -->
    <DatePicker
        android:id="@+id/datepicker_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    
    
    <TextView
        android:id="@+id/textview_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="请选择时间......" />
    <TimePicker 
        android:id="@+id/timepicker_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</LinearLayout>